<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.glassdashboard.mapper.CalendarEventMapper">

    <insert id="insert" parameterType="CalendarEvent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO calendar_events (title, start_date, end_date, color)
        VALUES (#{title}, #{startDate}, #{endDate}, #{color})
    </insert>

    <update id="update" parameterType="CalendarEvent">
        UPDATE calendar_events
        SET title = #{title},
            start_date = #{startDate},
            end_date = #{endDate},
            color = #{color}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM calendar_events WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultType="CalendarEvent">
        SELECT id, title, start_date as startDate, end_date as endDate, color
        FROM calendar_events
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="CalendarEvent">
        SELECT id, title, start_date as startDate, end_date as endDate, color
        FROM calendar_events
    </select>

</mapper>

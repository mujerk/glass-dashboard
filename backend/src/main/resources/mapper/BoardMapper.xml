<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.glassdashboard.mapper.BoardMapper">

    <resultMap id="BoardResultMap" type="Board">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="viewCount" column="view_count"/>
        <association property="author" javaType="User">
            <id property="id" column="user_id"/>
            <result property="username" column="username"/>
            <result property="name" column="name"/>
            <result property="role" column="role"/>
        </association>
    </resultMap>

    <insert id="insert" parameterType="Board" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO boards (title, content, user_id, created_at, view_count)
        VALUES (#{title}, #{content}, #{author.id}, NOW(), #{viewCount})
    </insert>

    <update id="update" parameterType="Board">
        UPDATE boards
        SET title = #{title},
            content = #{content},
            view_count = #{viewCount}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM boards WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="long" resultMap="BoardResultMap">
        SELECT b.id, b.title, b.content, b.created_at, b.view_count,
               u.id as user_id, u.username, u.name, u.role
        FROM boards b
        LEFT JOIN users u ON b.user_id = u.id
        WHERE b.id = #{id}
    </select>

    <select id="findAllByOrderByCreatedAtDesc" resultMap="BoardResultMap">
        SELECT b.id, b.title, b.content, b.created_at, b.view_count,
               u.id as user_id, u.username, u.name, u.role
        FROM boards b
        LEFT JOIN users u ON b.user_id = u.id
        ORDER BY b.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM boards
    </select>

</mapper>
